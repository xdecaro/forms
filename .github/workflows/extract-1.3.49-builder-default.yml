name: Extract Forms 1.3.49 Builder Default

on:
  push:
    paths:
      - 'tools/extract-1.3.49-builder-default.sh'
      - 'releases/_extract1349builder/READY'

permissions:
  contents: write

jobs:
  extract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Extract Builder default.php
        run: bash tools/extract-1.3.49-builder-default.sh
      - name: Commit extracted Builder
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tools/extracted-1.3.49-builder-default.php.txt
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Extract Forms 1.3.49 Builder default"
          git pull --rebase origin main
          git push
